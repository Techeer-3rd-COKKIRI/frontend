import React from 'react';
import styled from 'styled-components';

const NavBar = styled.div`
  width: 240px;
  height: 100vh;
  background-color: rgba(233, 237, 247, 1);
  font-size: 3rem;
`;
const Nav = () => {
  return <NavBar>난 nav야</NavBar>;
};

export default Nav;
